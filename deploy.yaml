kind: Deployment
apiVersion: apps/v1
metadata:
  name: loan-java
spec:
  replicas: 1
  selector:
    matchLabels:
      app: loan-java-app
  template:
    metadata:
      labels:
        app: loan-java-app
    spec:
      containers:
        - name: loan-java
          image: loan-java-image:latest
          ports:
            - name: http
              containerPort: 8081
              protocol: TCP
          resources:
            requests:
              memory: "300Mi"
              cpu: "50m"
---
kind: Service
apiVersion: v1
metadata:
  name: loan-java-svc
spec:
  ports:
    - name: http-8081
      port: 8081
      protocol: TCP
      targetPort: 8081
  selector:
    app: loan-java-app